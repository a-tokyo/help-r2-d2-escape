/* @flow */

import { GameElement } from '../';

// TeleportalGameElement

export default class TeleportalGameElement extends GameElement {
  constructor(name: string = 'teleportal0') {
    super('teleportal', name, false);
  }
}
